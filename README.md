# 🎵 MP3 Player

A simple and interactive web-based MP3 Player built using HTML, CSS, and JavaScript.  

This project demonstrates how to use the HTML5 `<audio>` element along with JavaScript to create custom audio controls and real-time playback tracking.

---

## 📌 Project Overview

The MP3 Player allows users to:

- ▶ Play audio
- ⏹ Stop audio
- ⏪ Rewind 10 seconds
- ⏩ Forward 10 seconds
- ⏱ View real-time playback duration

The player uses JavaScript to control audio playback and dynamically update the time display.

---

## 📂 Project Structure


MP3-Player/
│
├── index.html # Main HTML file
├── styles.css # Styling file
├── script.js # JavaScript logic
└── your_song.mp3 # Audio file (add your own MP3)


---

## 🛠️ Technologies Used

- HTML5
- CSS3
- JavaScript (Vanilla JS)
- HTML5 Audio API

---

## 🔎 Features

- Custom audio control buttons
- Time formatting (MM:SS)
- Forward & rewind functionality
- Responsive centered layout
- Clean modern UI

---

## ⚙️ How It Works

- The `<audio>` element handles media playback.
- JavaScript listens to button click events.
- `audio.currentTime` controls forward/rewind.
- `timeupdate` event updates playback time.
- `loadedmetadata` displays total duration.

---

## 🚀 How to Run the Project

1. Download or clone the repository:
   ```bash
   git clone git@github.com:AnshuKataria10/MP3-Player.git

Open the project folder.

Add your MP3 file and rename it:

your_song.mp3

Open index.html in your browser.

That’s it 🎵

🔮 Future Improvements

Add volume control slider

Add progress bar seek feature

Add playlist support

Add dark mode UI

Add animated music visualizer

👨‍💻 Author

Anshu Kataria
B.Tech CSE (DS)
